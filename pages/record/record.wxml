<!--pages/record/record.wxml-->
<navigation id='Navigation' show-icon='{{showIcon}}' title="{{navTitle}}">
</navigation>

<view class="container">
  <view class="sticky-box">
    <v-sticky bindmyevent="navCall" id="sticky" navbarInitTop="{{navbarInitTop}}" navArr="{{navArr}}" isFixedTop="{{isFixedTop}}"></v-sticky>
  </view>
  <product-list productList="{{productList}}"></product-list>
</view>